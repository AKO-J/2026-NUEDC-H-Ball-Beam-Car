# 当前状态（重新梳理前归档）

更新时间：2026-08-01

> 三功能按钮统一固件已完成软件整合：`build/competition_unified.out` 将第二题、第三题和第4/5/6问联合程序以 `Task2_run()`、`Task3_run()`、`Joint456_run()` 原入口封装接入。选择页S2/S3/S4选功能、S1确认进入、S5保留；进入后由原程序完整接管按键。问题2新增运行中S1急停，计时从实际发车开始并在自动停车或急停同周期冻结；OLED分别显示 `TASK2 LAP`、`TASK3`、`T456`。完整构建与主机回归通过，尚未烧录或实物验证。

- 已新增根目录 `系统整合与现场操作指南.md`，统一整理系统结构、关键接线、安全门、第三题/第二题/联合固件构建烧录入口、三次水平标定、K230 三点标定、CSV/分析/连续评分命令及现场故障判断。文档明确区分已验证能力与待实测能力；后续现场整合优先按该文档执行，当前任务和候选参数仍以 `CURRENT_TASK.md` 与源码为准。

> 五键最终定义：S1=选择确认/任务内部阶段推进/运行中急停，S2=第二问，S3=第三问，S4=第4/5/6问联合功能，S5保留。S4进入后完整保留原四次S1及S2/S3调平流程。

> 第4、5、6问当前联合固件、参数、证据边界和下一次实验已汇总至 `JOINT_456_HANDOFF.md`；后续以该文件顶部当前哈希为联合任务交接基线。

> 用户确认第4、5、6问已经线下直接调试通过。当前交付目标改为按钮统一固件接入；按钮人员必须阅读`BUTTON_INTEGRATION_HANDOFF.md`，只负责选择和进入现有联合功能，不重新调参。

- 当前联合固件将计划加速度前馈起点由第四次S1后的1.0秒提前到0.6秒；固定60 mdeg预倾仍立即生效，PWM仍在750 ms升至4%/5%、保持至1.0秒后跨越起转阈值。其他参数不变。构建和主机回归通过，SHA-256为`9f9fc227388571d035990aa8bdb25230367a929b541907291294654bc1ba2f60`。用户确认安全条件后已完成XDS110烧录和芯片内verification。

- 用户最终选择只增加启动步进前馈：车辆`ACCEL_LIMIT_X100`恢复为100，启动预倾由55提高至60 mdeg；`KFF_MILLI=1000`、Jerk、PWM曲线、循迹和小球PD均不变。构建和主机回归通过，SHA-256为`80044dc5cc0f740d0ddae0023286baf1e863a0a16c7971da905d7d8d87beb814`。用户确认安全条件后已完成XDS110烧录和芯片内verification。

- 当前待烧录候选将起步加速度设为20 ms整数离散下最小能实际加快PWM曲线的`ACCEL_LIMIT_X100=150`，只将启动预倾从55提高到60 mdeg；`KFF_MILLI=1000`保持不变，未恢复此前1100前馈。构建和主机回归通过，SHA-256为`0eadd538e3264cdc7bf3fdf45dc5801cc0d1419ac010974b120ade86b3182dcc`，尚未烧录或实测。

- 已按用户要求撤销最近一次`ACCEL_LIMIT_X100=150`、`KFF_MILLI=1100`调整，源码和构建产物恢复为上一版`ACCEL_LIMIT_X100=100`、`KFF_MILLI=1000`，SHA-256重新匹配`04b3cbcd3f3d4118db92c03c55034ec3f2c6fb3fa82af9b2aec8c221b7dd49e2`，完整回归通过。用户确认安全条件后，恢复版已重新烧录并通过芯片内verification。

- 当前联合固件只提高起步计划加速度和对应前馈：`ACCEL_LIMIT_X100=150`、`KFF_MILLI=1100`，`JERK_LIMIT_X100=60`及55 mdeg预倾不变。离散仿真S曲线运动段约5.32秒达到巡航，理论最大前馈约96 mdeg；构建和主机回归通过，SHA-256为`8c46f8d555cef8d92783aa09ca50abe71e440955677cc848dda72293fe30019e`。用户确认安全条件后已完成XDS110写入和芯片内verification，尚未实测。

- 最新候选加入静摩擦死区启动前馈：第四次S1后用750 ms将左右PWM分别升至4%/5%，保持250 ms并预倾杆`55 mdeg`，随后跨至已测起转值5%/6%，再运行原S曲线；计划加速度按`KFF_MILLI=1000`换算杆角并与视觉PD叠加。完整构建和主机回归通过，SHA-256为`04b3cbcd3f3d4118db92c03c55034ec3f2c6fb3fa82af9b2aec8c221b7dd49e2`。用户确认安全条件后已完成XDS110写入和芯片内verification；`KFF_SIGN=+1`仍必须先架空验证。

- 当前最新联合映像将缓启动参数降为`ACCEL_LIMIT_X100=100`、`JERK_LIMIT_X100=60`，离散仿真约`6.48 s`达到巡航。映像SHA-256为`8b89163294dd4f290886294b696731e044abe94fce535cfc202104009dfded37`，已在用户确认安全条件后烧录并通过芯片内verification；实际起步加速度仍待CSV实测。

## 第4、5、6问联合能力最新结论

- 第4、5、6问所需的共同执行链路已经具备：第二问循迹状态表、约4.5秒S曲线缓启动、K230钢球位置闭环、步进杆角控制、OLED分阶段操作、S1运行中急停以及CSV遥测。
- 按现场最新决定，当前联合映像取消全部里程/终点自动停车；第四次S1后持续循迹，编码器只保留距离与速度遥测，车辆仅由运行中再次按S1停止。
- 最新映像 `motor_forward_test/build/joint_line_balance_test.out` 已在用户确认TB6612 VM断开或车轮悬空的条件下完成XDS110写入和芯片内verification；SHA-256为 `ebb57298ae8b6a53b324ec8a40591a479085ce8798b1f59e04db711c9329ace2`，目标未由烧录器自动启动。
- 项目交接口径：第4、5、6问均具备实现能力，可以分别进入实车测试；但构建和烧录不能替代每一问的全程实测。在取得对应CSV、meta和分析曲线前，不标记为实物验收通过。

## 直线巡线与小球平衡联合固件（本轮新增，尚未实机验证）

- 新增独立映像 `motor_forward_test/build/joint_line_balance_test.out`，没有替换第二题、第三题或第四题已有映像。
- 当前候选结构为：LF04加权误差PD直接经S曲线速度参考映射为左右开环PWM；编码器仅用于测速日志、实际加速度前馈和异常检测，不再参与车轮PID。仍保留限加速度/限jerk速度剖面、加速度到杆角前馈、视觉±1 cm监测、受控停车和故障锁存。
- 新增37列CSV记录器、四联分析图和连续5次评分工具；联合固件现直接复用第三问的α-β状态估计与位置PD，在第三次S1后的READY和车辆运行阶段维持保存点，并与加速度前馈叠加，不使用积分项。
- 联合映像已补入SSD1306 OLED操作引导：调平时提示S2/S3和S1保存，视觉未就绪时提示检查K230/摆球居中，可启动时提示S1，运行时提示S1急停，受控停车、完成和各类故障均显示明确下一步；关键遥测保留在剩余行。OLED初始化位于所有可能重置GPIO的模块之后。
- 联合固件的起始调平已改为复用第三题已验证流程：机构人工停在下机械端且软件POS=0，第一次S1解锁校准，第二次S1执行受限 `-565` 步预置抬升，完成后S2/S3仅在 `-565±132` 安全包络内微调，最后S1保存水平并配置相对软限位。当前位置不再被直接误存为水平。
- 第三题式调平版已通过XDS110烧录和芯片内verification；固件SHA-256为 `8ebfb6f89fe05984ece4e22621dfcbcfb97bcd0083d6492b9ce5d0eea4103aa0`。调试器未自动启动，待复位后按OLED分阶段验证。
- 下一候选版将第三次S1扩展为“保存杆水平＋当前有效视觉小球位置”：只有K230新鲜有效、且当前物理K230零位落在±1 cm内才允许保存；后续球误差、±1 cm保护和可选视觉PD以保存点为零，保存后重新计0.5秒稳定时间。CSV增加 `ball_target_cm_x100`，schema升为v2。该版已构建和主机回归通过，尚未烧录。
- OLED补充后的 `make -B joint-line-balance-test check-host` 已通过；这只证明构建和主机逻辑回归，不证明OLED实物通信、电机方向、前馈符号、5 cm/s候选速度、PI/PD参数或±1 cm目标已通过实物验证。
- OLED操作引导版联合映像已在用户确认车轮悬空或TB6612 VM断开后通过XDS110烧录，芯片内verification成功；SHA-256为 `d12b00f135c8c9a4b7a15be1cd641c95f067785d053ce54ac5efe386ff7219b2`。调试器未自动启动目标，仍待复位后的OLED和静止日志验证。
- 当前候选参数全部标记为 `UNVERIFIED`，不会自动改写。烧录前仍须车轮悬空或断开TB6612 VM，并按架空方向/急停/传感器/编码器/步进符号顺序验证。
- 当前视觉PD候选映像已在用户确认TB6612 VM断开后通过XDS110烧录及芯片内verification；SHA-256为 `efe79e1f376efb4571f16bdd524cab1b969fabd88dc45e36e2f8571f3c69e1e1`。调试器未自动启动目标；尚未完成复位后的静止检查、小球固定点闭环或车辆加减速扰动实验，因此不能认定±1 cm通过。
- 首次READY静态回中日志 `logs/joint/joint_20260801_152227.csv` 已完成CSV/meta/分析图闭环：从 `+2.63 cm` 扰动回到并持续位于±1 cm约需 `1.10 s`，最小回摆 `-1.08 cm`，末段1秒最大误差 `0.31 cm`；PD输出 `-384..+191 mdeg`，无视觉陈旧样本或故障。实物方向正确，但用户判定响应偏慢。
- 本轮只提高联合映像比例增益：等效Kp由 `11` 改为 `14 mdeg/px`，Kd仍为 `1 mdeg/(px/s)`，积分仍为0，方向和±400 mdeg限幅不变。共享第三题控制器Kp=11未修改；联合Kp=14映像SHA-256为 `bbfa6fa684ba8f8090ac938293a63e89de462cb70f97f8d6292abd013634591d`，已在VM断开条件下烧录并通过芯片内verification，尚待同样约2 cm扰动复测。
- Kp=14静态复测出现最大`10.56 cm`往返且PD双向撞到±400 mdeg，判定失稳并停止，不采用。当前只将联合Kp降至`12 mdeg/px`，Kd和其余参数不变；映像SHA-256为`1f7bd2ac9245f917d4fb8a25a51c29a2885535a5c68e7e25dbcc47bdb38a7a96`，已在VM断开条件下烧录并通过verification，待独立复测。
- Kp=12复测也出现持续回摆：一度回到`-0.08 cm`后又自行到`+1.76 cm`，因此不采用。当前已回退到唯一实测稳定的联合Kp=`11 mdeg/px`，Kd=`1`、积分0；映像SHA-256为`08b7899f327a5c6089ac422303f3c1d14f18a51d630ece7009c83bdfa7da22b6`，已在VM断开条件下烧录并通过verification，目标未自动启动。下一阶段只在正常平直、受限S曲线加速度工况验证。
- 首次架空PWM运行 `logs/joint/joint_20260801_154112.csv` 中，左右轮分别约在5%/6%开始产生正确的负向编码器计数，PWM按1%/20 ms平滑上升；小球运行段最大误差0.54 cm且无视觉陈旧样本。但旧堵转检测按单个20 ms样本累计零增量，在低速编码器间歇采样下于8% PWM误触发故障8。现改为PWM≥8%后分别在完整500 ms窗口检查每轮累计至少4个负计数；PWM参数未改。修正版SHA-256为`d6eb0410121e4677751c127c5da8bc11da5f740f63a7126b1c5a652f1adf61d3`，已在车轮悬空条件下烧录并通过verification，待复测。
- CanMV IDE现场状态已确认K230连接且视觉脚本曾以约21–22 FPS运行；当前回到REPL。尚未从设备文件树确认 `/sdcard/main.py`，因此不能认定脱机自启动已经配置。项目视觉源文件已加入可中断的3秒启动窗口，仍需通过“CanMV：另存为 main.py”写入设备后断电验证。
- 随后通过Mac直读K230 SD卡确认根目录原先没有 `main.py` 和 `boot.py`。当前视觉脚本已写入SD卡根目录 `main.py`，ROI配置同步更新，模型哈希与项目一致；原脚本和配置均保留为 `*.backup_before_main_20260801.py`。文件写入已校验，但断电自启动仍待装回K230后验证。

## 系统目标

视觉检测摆杆内钢球位置，利用 X42S 闭环步进电机连续微调摆杆，使钢球稳定到可设定目标位置；所有结论以现场实验日志为准。

## 当前硬件（已从工程文档确认）

- 主控：TI LP-MSPM0G3507。
- 视觉：K230，经 UART3 单向发送钢球位置至 MSPM0；工程配置为 921600 baud。
- 执行器：X42S 闭环步进电机，`PB13/PB12/PB6` 分别用于 `Stp/Dir/En`。
- 车轮：TB6612 双电机与双编码器；与摆杆闭环分开。
- 通信与电源的完整边界见 `motor_forward_test/H_TASK_AI_HANDOFF.md`；未在该文档中验证的实物连接不得视为已完成。

## 已验证

- 第二题正式循迹映像 `build/line_follow_test.out` 已于 2026-08-01 重新构建，`make check-host` 全部通过，并经 XDS110 写入及芯片内 verification 成功；SHA-256 为 `d86eeca8afb5dcd4d5913c1ff76bb6f4cdea508ea74389cf4e1e737fc71a639a`。目标未自动启动，实车循迹结果尚未验证。

- 第四题联合测试映像 `build/task4_baseline_test.out` 已构建并通过 XDS110 烧录校验；修正中心目标初始化后的固件 SHA-256 为 `6ad8412f7d4c5a06daba3b1f0ca9729b8d7ef131edfe9ee3c91c69fa2399a939`。烧录时用户确认车轮悬空；TB6612 电机电源已接通。调试器未自动启动目标，需再次复位。
- 首次复位后的静止日志 `logs/task4/task4_20260801_130806.csv` 共 72 行：双轮 PWM、编码器计数和估算距离均为 0，IMU 无缺失，状态保持 `DISARMED/WAIT_LEVEL`，上电静止联锁通过；K230 有效视觉样本为 0，因此禁止启动剖面。该日志对应修正前中心目标映像，只用于静止安全和链路诊断，不用于钢球误差评价。
- 第四题 CSV v1 记录器 `tools/record_task4_serial.py` 与分析器 `tools/analyze_task4.py` 已新增，23 列格式包含钢球厘米误差、双编码器速度、三轴加速度、双轮 PWM、运行阶段和安全状态；合成数据端到端生成曲线/摘要通过，实机日志仍待采集。

- 第四题低速直线 AB 的独立速度剖面状态机已加入：默认 `1.5 s` 线性起步、`2.0 s` 匀速、`1.5 s` 线性减速，最高 `22% PWM`；具备显式待命、单次启动、急停后不自动重启及计时器回绕测试。`make check-host` 已通过。该结果仅为主机侧软件验证，尚未烧录或驱动车轮。
- 第三题 `0→+5→-5 cm` 已正式通过；现场确认使用冻结参数完成 3 次重复性验证。当前可唯一追溯的详细通过记录为 `run_20260801_123833.csv`；其余两次由现场确认，目录中尚未识别出对应的独立日志文件名。
- 闭环诊断 CSV 可保存，`tools/analyze_latest.py` 可生成曲线、摘要和评分 JSON。
- 第三题单次实机验收通过：`run_20260801_123833.csv` 总时长 `1.75 s`，正端峰值 `+66 px`，负端停止 `-56 px` 且停止后保持 `-77..-56 px`，丢球和估算丢帧均为 `0`。参数冻结，K230 未修改。
- 已新增五键选择/急停/标定 UI 状态机：S1 为 START/急停，S2～S5 选择问题 2～5；待机长按 S1 2 秒进入标定，标定中 S2/S3/S4/S5 为 DOWN/UP/PRESET/SAVE。主机测试与无电机 OLED 测试固件构建通过，尚未烧录实测。
- 2026-08-01 现场视觉三点标定：物理 `-5 cm / 0 cm / +5 cm` 分别为完整画面 `X=84 / 149 / 208 px`；负侧为 `13.0 px/cm`，正侧为 `11.8 px/cm`。K230 显示换算与第三题目标已按非对称比例更新并通过构建/主机回归；采用 `+59/-65 px` 目标的新第三题映像已烧录并校验，K230 新脚本仍需确认上传生效。
- `ball_beam_vision_control_test.c` 已改为上电时保持 X42S 驱动未使能；首次物理 `START` 才进入受保护的校准解锁路径。主机侧 `make check-host` 已通过，且该变更随第三题映像烧录；仍不能替代实物上电验证。
- 第三题专用映像已于 2026-08-01 按现场三点标定烧录、校验并完成动态验收；冻结参数为正向巡航 `+800 mdeg`、正向提前反向阈值 `+12 px`、负向反向制动 `+565 mdeg`。
- `run_20260801_073324.csv` 记录到 36.6 s 的 `ACTIVE` 状态，未见 `LOST` 或故障状态，且 `motor_execution_observed=true`。
- 当前闭环日志字段已包含位置、误差、控制量、目标/估计电机位置、步进频率及状态。
- 主机侧控制器测试入口已存在：`make check-host`。

## 尚未验证 / 仍受限制

- K230 水管 ROI 已从 `y=52..99` 收紧为 `y=58..93`，以移除截图中上下背景杂物；尚未上传实测，须确认全行程钢球框未被裁边，并重新核对 `0 cm` 与 `±5 cm` 像素位置。
- 像素误差与实际摆杆运动/钢球位移的定量映射。
- “钢球偏左/偏右 → 正确电机方向”的实物方向验证。
- 控制周期的稳定性：最新日志中视觉间隔统计异常（均值 1846.13 ms、最大值 24136643 ms），须以一次干净采集复核。
- 安全人工验证尚未记录；评分文件明确锁定自动 PID 改写。
- 最新记录没有目标设定值阶跃，不能计算标准阶跃过冲、稳定时间或稳态误差。

## 当前主要问题

用户已明确把当前任务从第四题切换为第二题实车测试。第二题固件已经安全烧录并保持未启动；下一步先做复位后的架空待机、方向、传感器响应和停车手段检查，再进行落地一圈测试。未取得现场运行观察前，不把烧录成功视为第二题通过。

以下第四题状态暂时保留，待第二题测试结束后再恢复：

第三题已完成，当前切换到第四题。近期唯一任务是低速直线 AB 基线采集：先分离车辆起步、匀速和刹车三段，记录钢球位置、左右轮速、车辆加速度/IMU与摆杆命令的时间关系。尚未通过基线数据验证纵向扰动的符号、幅值和重复性，因此当前不加入加速度前馈、不修改 PID。

第四题联合固件与扩展 CSV 软件链路已完成并烧录校验；下一步是在保持车轮悬空且 TB6612 电机电源断开的条件下复位，先采集静止日志，再接通电机电源做悬空阶段切换与急停验证。不得把构建、烧录或合成日志测试视为实车基线完成。

以下为第三题已关闭的历史调试过程：

五键选择与 OLED 引导层已经完成，但问题 2 与问题 3 仍是两个独立固件，问题 4/5 的车轮与摆杆联合任务执行器尚未实现，因此当前选择层没有连接电机任务调度。先用不驱动电机的 `competition-keys-oled-test` 验证五个实物按键和 OLED，再进行统一固件架构整合。

2026-08-01 首条第三题动态记录 `run_20260801_113536.csv` 显示：球在约
0.9 s 内到达正端容差并折返，但负向制动在 `x=+30 px、v=-121 px/s`
触发后只维持一帧即回水平，球继续冲至约 `x=-144 px`，因此未通过。
状态机已改为持续保持 `+202 mdeg` 反向制动，直到球实际进入
`-65±13 px` 后才回水平；未修改 PID，构建和 `make check-host` 已通过。
首次复测中 `+0.70..0.715°` 未克服静摩擦，球保持在 `-6..-5 px` 直至超时；
现只将正向远端起步/巡航固定为已有 `+0.80°` 安全上限，不提高上限、不改 PID。
该起步修正使球 0.75 s 进入正端容差、2.10 s 首次进入负端容差，但 `+0.202°`
制动不足，入带速度约 `-266 px/s`，之后冲至 `-139 px`。现只把反向制动提高到
同一已有 `+0.80°` 安全上限；新映像已烧录并完成 verification，尚需重新校准复测。
双向 `0.80°` 复测已使负端回水平后保持 `-52..-68 px`，但正端仍从首次
入带继续冲到约 `+124 px`。现保留已验证的负端制动，只在 `x=+20 px`
提前切换到现有 `-0.828°`，利用惯性进入 +5 cm；映像已烧录并通过 verification。
提前折返复测将正端峰值从约 `+124` 降到 `+84 px`；但 `+0.80°` 负端制动
使最左点只到 `-48 px`，随后反向并超时。现仅将负端制动插值调整为 `+0.70°`，
新映像已烧录并通过 verification，待重新校准复测。
`+0.70°` 复测总时长 1.95 s，负端最终约 `-81 px`，只略超 1 cm；据
0.70/0.80° 两端实测将负端制动夹定为 `+0.75°`。该映像已烧录并通过 verification。
`+0.75°` 复测已验证负端回水平后保持 `-55..-64 px`、最终 `-62 px`，
总时长 2.00 s；仅正端峰值 `+85 px` 超限。现只将正端提前折返阈值由
`+20 px` 调到 `+5 px`，最终候选映像已烧录并通过 verification，待复测。
`+5 px` 阈值复测的正端峰值仅 `+39 px`、负端最终 `-58 px`；结合此前
`+20 px→峰值+85 px`，线性插值得到正端折返阈值 `+12 px`。新候选映像已
烧录并通过 verification，负端 `+0.75°` 参数保持不变。

## 当前代码入口

- 闭环应用：`motor_forward_test/tests/bringup/ball_beam_vision_control_test.c`
- 控制程序：`motor_forward_test/ball_beam_controller.c`
- 步进执行：`motor_forward_test/stepper_beam.c`
- 视觉：`motor_forward_test/k230/steel_ball_uart_yolo.py`
- 分析：`motor_forward_test/tools/analyze_latest.py`

## 最新实验

- CSV：`motor_forward_test/logs/run_20260801_123833.csv`
- 参数/来源哈希：`motor_forward_test/logs/run_20260801_123833.meta.json`
- 曲线：`motor_forward_test/analysis/run_20260801_123833_analysis.png`
# 2026-08-01 12:30 第三题 720 mdeg 候选已烧录

- 无接触实测 `run_20260801_122313.csv` 中，正向达到 `+59 px`；负向采用 `+750 mdeg` 制动时，小球最低约 `-39 px` 后真实快速回到右侧，判定制动过强。
- 仅将 `TASK3_NEGATIVE_REVERSE_BRAKE_MDEG` 从 `750` 改为 `720`；正向提前制动阈值保持 `+12 px`，PID 未修改。
- `make -B ball-beam-task3-test check-host` 通过；XDS110 烧录和程序校验成功。
- 固件 SHA-256：`478f68a84cb6aa883a31eafb3c54c86ae0d1f9f2f75fc640df93d9a66bceec0e`。
- 下一步：复位 TI 后重新完成三次水平校准，再执行一条无人触碰的第三题 CSV；验证正向 `+48..+70 px`、负向最终 `-52..-78 px`、总时长不超过 5 秒。
# 2026-08-01 12:40 第三题单次实机验收通过

- 有效日志：`motor_forward_test/logs/run_20260801_123833.csv`；对应参数：`logs/run_20260801_123833.meta.json`；曲线：`analysis/run_20260801_123833_analysis.png`。
- 固件参数：正向巡航 `+800 mdeg`、正向提前反向阈值 `+12 px`、负向反向制动 `+565 mdeg`；PID 未修改。
- 实测轨迹总时长 `1.75 s`，小于 5 秒。
- 正端峰值 `+66 px`（约 `+5.6 cm`），位于允许范围 `+48..+70 px`。
- 负端进入 HOLD 时 `-56 px`；停止后范围 `-77..-56 px`，全部位于允许范围 `-78..-52 px`。
- 本次有效测试丢球样本 `0`、估算丢帧 `0`，停止后无越界回弹；第三题单次真实闭环记录满足要求。
- 当前 TI 固件 SHA-256：`8a41e30bdc0f4f869a06118adf4c4ef0c4f9dc0365db7b0ab6aa4dba79bbfbdc`，XDS110 烧录校验成功。
- 若要宣称可重复通过，下一步应在不改参数的情况下再做至少 2 次无人触碰复测；当前不要继续调参。
