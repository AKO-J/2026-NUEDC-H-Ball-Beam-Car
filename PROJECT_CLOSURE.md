# SMARTCAR 项目结题与赛后复盘

日期：2026-09-05
结论：项目已完成比赛使命，获得省级电子设计竞赛三等奖，停止主动开发并归档。

## 一句话结论

本项目的软件链路和赛前分项实验形成了可复用成果，但赛场可靠性被关键支撑结构松动破坏；后续若重做，首要改进应是机械基准、微调分辨率和现场调试流程，而不是继续在原结构上堆叠 PID 参数。

## 已完成的成果

- 建立了 K230 视觉检测钢球、UART 传输、MSPM0 控制和 X42S 摆杆执行链路。
- 集成了 LF04 循迹、车轮驱动、编码器、OLED、按键选择、急停和统一比赛固件。
- 第三题存在完整的赛前通过样本；联合控制保留了 Kp=11/12/14 等决定性实验及失败证据。
- 形成了主机回归、日志采集、分析、评分、安全检查和交付包。
- 省级电子设计竞赛获得三等奖。

## 赛场失效复盘

### 已由用户确认的事实

- 支撑球杆机构的支架底部热熔胶出现松动。
- 机械基准变化后，赛场前段题目受到影响，第三题 PID 小球运动无法稳定完成预期动作。
- 5 度微调不够细，现场难以快速恢复到可用姿态。
- 调试方式偏逐项串行，时间消耗过大。

### 可以合理推断但未被测量证明的机制

- 控制器、视觉标定和端点参数都依赖固定的杆角度—像素—球位置映射；支架松动会使这些赛前参数整体失配。
- 微调粒度过大时，机械修正一次可能跨过有效区间，PID 很难用软件补偿不断漂移的机械零点。
- 赛场时间不足使重新标定、结构补强和控制复调无法形成完整闭环。

这些机制是工程推断，不作为已测得的唯一根因。没有赛场 CSV、位移测量和时间线，就不能证明某个 PID 增益或某一行代码直接导致失效。

## 最值得保留的教训

1. **机械完整性是闭环控制的前置条件。** 关键支撑不能只依赖热熔胶；应使用螺钉、夹具、定位销或至少具备机械限位的复合固定，并做拉扯/振动/运输后复检。
2. **把零点重复性纳入上电检查。** 每次搬运后先验证机械水平、端点、回差和 `-5/0/+5 cm` 三点映射；失败则禁止进入 PID 项。
3. **微调分辨率必须由指标倒推。** 不应直接用“5 度”作为方便的操作步长，应先计算该角度在球位置或像素上的变化，并提供粗调/细调两档。
4. **现场调试要有时间盒和降级策略。** 先判断结构、供电、传感器、执行器还是软件；超过预设时间仍不稳定就切换到可拿分的稳定功能，避免一路串行试错。
5. **赛前通过不代表运输后仍通过。** 至少增加“运输后快速复验”和关键紧固点标记；用漆线、照片或刻线检查是否位移。
6. **证据要覆盖比赛固件和最终结构。** 分项样本有价值，但不能代替统一映像、最终安装姿态和比赛现场流程的验收。

## 归档保留集

- 根目录状态、结题、核心文档、实验索引和安全/接线资料。
- `motor_forward_test/` 的当前源码、Makefile、K230 脚本/配置/当前模型、主机测试和必要分析工具。
- `build/competition_unified.out` 及 SHA-256。
- `run_20260801_123833` 与五组关键联合实验的 CSV、meta、分析图/摘要。
- 设计报告、核心源码和决定性实验数据。
- GitHub 脱敏归档 `releases/SMARTCAR_GitHub_archive_20260905.zip` 及同名 SHA-256 文件；该包不含虚拟环境、训练资产和可重建缓存。
- 获奖证书、官方结果页或现场照片一旦取得，应另存到 `deliverables/competition_evidence/`，并在本文件注明来源。

## 不属于核心证据的内容

- Python 虚拟环境、软件安装镜像、缓存、临时渲染目录和可重建的编译中间文件。
- 可从原始数据和配置重新生成的训练缓存或重复预览。

删除前仍应确认最终轻量归档可解压、哈希一致，并至少复制到另一个物理位置或云端。当前结题动作不删除原文件。

## 实体硬件收存

- 断开电池和 TB6612 电机电源，取下钢球并固定摆杆等活动部件；锂电池按其规格单独安全存放。
- 给线束、K230、MSPM0、X42S、传感器和电源端拍一组清晰照片并贴标签，避免以后只能依赖记忆复原。
- 在松动支架上明确标记“机械基准已失效，禁止直接沿用旧标定/PID”，不要把当前装配状态误认为比赛前的有效结构。

## 如果未来重启

项目必须从“机械整改与重新标定”重新开始：固定支架 → 测零点/回差 → 验证微调分辨率 → 重做视觉三点映射 → 开环安全测试 → 静态闭环 → 动态任务。旧 PID 参数只能作为参考起点，不能直接视为可用参数。

## 证据口径

- 比赛名次、赛场故障和调试体验：用户现场确认。
- 构建与主机测试：2026-09-05 本地重新执行通过。
- 烧录、芯片校验和实物验收：仍按旧记录冲突或未验证处理。
- 所有未完成项在结题后冻结，不改写为成功，也不继续消耗项目时间。
