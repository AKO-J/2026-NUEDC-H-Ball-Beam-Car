# 第4、5、6问联合固件交接

更新时间：2026-08-01

## 当前结论

第4、5、6问所需的共同软件与硬件控制链路已经完成整合。用户确认已经线下直接调试通过；该结论属于用户现场确认。当前工作不再继续追查历史日志，转为把现有通过代码交给按钮统一固件接入。按钮整合要求见`BUTTON_INTEGRATION_HANDOFF.md`。

已经冻结的第二题、第三题和第四题独立固件未被修改；本轮只使用独立联合映像：

`motor_forward_test/build/joint_line_balance_test.out`

当前映像SHA-256：

`9f9fc227388571d035990aa8bdb25230367a929b541907291294654bc1ba2f60`

该映像已在用户确认车轮悬空或TB6612 VM断开后通过XDS110烧录和芯片内verification。调试器未自动启动目标，复测前需要复位或重新上电。

## 当前操作流程

1. 第一次S1：解锁调平。
2. 第二次S1：从下机械端软件POS=0相对运行约`-565`步。
3. S2/S3：人工微调杆水平。
4. 第三次S1：保存当前杆水平和当前有效视觉小球目标；K230缺失不会阻止后续启动。
5. READY阶段继续使用视觉PD保持保存点。
6. 第四次S1：进入启动预倾、PWM死区预升和持续循迹。
7. 运行中再次按S1：急停。

当前没有1.5米、整圈或其他里程自动停车。编码器只用于速度、距离和实际加速度遥测，不参与停车和车轮速度PID。

## 车轮控制

控制链路：第二问LF04循迹状态表 + 共同S曲线倍率 → 左右开环PWM。

- 巡航参考：`CRUISE_SPEED_X100=500`。
- 最大计划加速度：`ACCEL_LIMIT_X100=100`，即`1.00 cm/s²`。
- Jerk限制：`JERK_LIMIT_X100=60`，即`0.60 cm/s³`。
- 第四次S1后0～750 ms：左/右PWM分别预升至4%/5%。
- 750～1000 ms：保持4%/5%。
- 1000 ms：跨至已测起转值5%/6%，随后继续S曲线。
- 丢线沿用第二问控制器的保持上一命令逻辑。
- 无自动停车；只有运行中S1急停。

## 小球与步进控制

- 视觉PD：复用第三问的α-β估计器和`BallBeamController`。
- `Kp=11 mdeg/px`，`Kd=1 mdeg/(px/s)`，积分为0。
- `BALL_CONTROL_SIGN=+1`。
- 视觉PD限制为±400 mdeg。
- 总杆角限制在实测标定范围`-2221..+1777 mdeg`，不外推标定表。
- 第四次S1后固定启动预倾立即设为`60 mdeg`。
- 计划加速度前馈从第四次S1后的`0.6 s`开始。
- 前馈比例`KFF_MILLI=1000`，方向`KFF_SIGN=+1`。
- 计划前馈与视觉PD叠加后再经过机械角度限幅。

`KFF_SIGN=+1`仍需由当前固件的架空实验确认。若方向错误，只修改`KFF_SIGN`，不得同时修改增益、预倾角或小球PID。

## 已验证事实

- 当前源码完整构建通过。
- `make -B joint-line-balance-test check-host`通过。
- 当前映像XDS110写入和芯片内verification成功。
- 历史静态Kp=11实验中，小球从约`+2.63 cm`扰动返回±1 cm约需`1.10 s`；Kp=12和14均出现不可接受回摆，因此不采用。
- 历史架空实验观察到左轮约5%、右轮约6%开始产生正确方向的编码器计数，这是当前死区启动值的依据。

## 日志证据边界

- 用户已确认当前功能线下直接调试通过。
- 该现场结论没有在本项目目录中补造新的CSV、meta或分析曲线；文档明确记录为用户确认，不冒充工具分析结论。

## 下一步：按钮整合

1. 按`BUTTON_INTEGRATION_HANDOFF.md`把S4映射为第4/5/6问联合功能。
2. 原样保留联合固件的四次S1、S2/S3调平、第四次启动和运行中急停状态机。
3. 按钮层不得修改已通过的控制参数。
4. 完成选择层整合后做架空按键流程和急停检查。

## 验收表述

- 用户现场确认表述：第4、5、6问已经线下直接调试通过。
- 工具证据表述：当前目录没有为这次线下通过补造新的CSV、meta和分析曲线。

## 安全边界

- 烧录和首次电机测试前，车轮必须悬空或TB6612 VM断开。
- 每次闭环实验必须保存CSV、meta和分析曲线。
- 没有安全验证不得自动调整PID。
- 实验一次只修改一个参数组；方向错误时只修改符号。
