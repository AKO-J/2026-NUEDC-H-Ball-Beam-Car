# 按钮统一固件：第4、5、6问功能接入说明

更新时间：2026-08-01

## 交付结论

按钮统一固件的软件整合已经完成。统一入口为
`motor_forward_test/competition_selector_main.c`，构建产物为
`motor_forward_test/build/competition_unified.out`。选择层只调用
`Task2_run()`、`Task3_run()` 或 `Joint456_run()`；进入后仍由原程序完整接管
按键与安全流程。当前只完成构建和主机回归，尚未烧录或实物验收。

第4、5、6问共用当前联合控制代码。用户已现场确认线下直接调试通过，按钮整合人员不得重新设计控制器、重新调参或用旧版本替换。

通过代码入口：

`motor_forward_test/tests/bringup/joint_line_balance_test.c`

当前构建产物：

`motor_forward_test/build/joint_line_balance_test.out`

当前SHA-256：

`9f9fc227388571d035990aa8bdb25230367a929b541907291294654bc1ba2f60`

按钮层已有参考：

- `motor_forward_test/competition_ui.c`
- `motor_forward_test/competition_ui.h`
- `motor_forward_test/tests/bringup/competition_keys_oled_test.c`

## 按钮映射

统一选择页：

- S1 / PB7：确认、阶段推进、运行中急停。
- S2 / PB8：选择第二题；进入4/5/6调平后为按住下降。
- S3 / PB15：选择第三题；进入4/5/6调平后为按住上升。
- S4 / PB0：选择第4/5/6问联合功能。
- S5 / PB20：保留给统一标定/返回功能，不参与当前联合控制流程。

## S4选择后必须保留的联合流程

S4只选择功能，不能直接启动车轮。进入4/5/6联合模式后：

1. 第一次S1：解锁调平，车轮保持停止。
2. 第二次S1：步进电机从下机械端软件POS=0相对抬升约`-565`步。
3. 抬升完成后，S2/S3按住进行水平微调。
4. 第三次S1：保存杆水平与当前视觉小球目标，进入READY。
5. 第四次S1：执行启动预倾、PWM预升和循迹运行。
6. 运行中再次按S1：立即急停车轮，停止并禁用步进输出。

第三次S1后不以K230是否收到数据阻止第四次S1。K230有数据亮绿灯，无数据亮红灯；有有效目标时继续使用视觉PD，没有有效目标时不应阻止车辆循迹。

## 当前通过参数，不得由按钮层修改

- 小球PD：`Kp=11 mdeg/px`、`Kd=1 mdeg/(px/s)`、I=0。
- `BALL_CONTROL_SIGN=+1`。
- 视觉PD限制：±400 mdeg。
- 车辆巡航参考：`CRUISE_SPEED_X100=500`。
- 加速度：`ACCEL_LIMIT_X100=100`。
- Jerk：`JERK_LIMIT_X100=60`。
- 启动固定预倾：`60 mdeg`，第四次S1后立即生效。
- 计划加速度前馈：第四次S1后`0.6 s`开始。
- `KFF_MILLI=1000`，`KFF_SIGN=+1`。
- PWM在750 ms内升至左4%/右5%，保持至1.0 s，再跨至左5%/右6%。
- 运行后持续使用第二问循迹逻辑，无里程或终点自动停车。

## 推荐的最低风险接法

不要把联合状态机拆散后重新写进`competition_ui.c`。推荐只做入口封装：

1. 将联合文件中的`main()`机械性改名为`Joint456_run()`，控制逻辑不改。
2. 新建对应头文件，只暴露`void Joint456_run(void);`。
3. 统一按钮主程序在选择S4后调用`Joint456_run()`。
4. 进入后由联合代码自己读取S1/S2/S3并完成上述全部阶段。
5. 运行中S1急停仍由联合代码最高优先级处理。
6. 如需重新选择题目，急停后要求人工复位，不在第一版整合中增加复杂的运行时返回。

这样可以最大程度保持线下已经通过的控制路径，只把“选择哪一个功能”放到按钮层。

## 如果必须做成非阻塞统一调度

只有在统一主循环不能调用独占入口时，才把联合代码拆成以下接口：

```c
void Joint456_init(void);
void Joint456_onStartEdge(void);
void Joint456_setJogKeys(bool downHeld, bool upHeld);
void Joint456_service5ms(void);
bool Joint456_isRunning(void);
void Joint456_emergencyStop(void);
```

拆分时必须保持原来的5 ms节拍、20 ms速度更新、10 ms IMU更新和50 ms日志更新，不得把控制逻辑放进按键中断。

## 按键层实现要求

- 所有按键低电平有效、上拉输入。
- S1只在稳定按下边沿触发一次；释放稳定至少30 ms后才允许下一次边沿。
- S2/S3微调使用稳定按住状态，不使用重复按下边沿。
- 运行中S1急停优先级高于OLED、功能选择、视觉处理和日志。
- 急停必须调用`Motor_stopAll()`、`StepperBeam_stop()`并禁止步进输出。
- 功能切换时不得保留上一次S1计数、步进移动状态、编码器计数或故障锁存。

## 不允许的整合方式

- 不允许S4一按就直接启动车轮。
- 不允许把`COMPETITION_ACTION_START_TASK`直接等同于联合固件第四次S1。
- 不允许删除前三次S1调平流程。
- 不允许按钮层改写小球PID、前馈、PWM、机械标定或循迹参数。
- 不允许重新链接归档目录中的历史代码。
- 不允许修改已冻结的第二题、第三题和第四题独立固件。

## 整合验收清单

- 上电停留在选择页，车轮和步进均不动作。
- S4选择后显示第4/5/6问联合功能，但车轮仍不动作。
- 前三次S1与S2/S3调平流程和当前独立联合固件一致。
- 第四次S1后按当前时序预倾并启动循迹。
- 运行中S1能够立即急停。
- K230缺失只改变红/绿状态灯，不阻止第四次S1。
- 无自动停车。
- 整合构建通过`make check-host`。
- 实物验证前车轮悬空或TB6612 VM断开。

## 最终对外表述

第4、5、6问功能已经在线下直接调试通过；按钮整合只负责选择并进入现有联合功能，不重新调整控制参数。
