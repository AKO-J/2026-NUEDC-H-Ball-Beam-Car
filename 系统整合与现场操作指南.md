# SMARTCAR 系统整合与现场操作指南

更新时间：2026-08-01

## 1. 文档用途

本文用于把目前分散的底盘循迹、K230 钢球视觉、X42S 摆杆、OLED、按键和日志工具整合成一套可重复的现场流程。

使用顺序：

1. 先看根目录 `CURRENT_TASK.md`，确认本轮只测哪一项。
2. 再看 `PROJECT_STATUS.md`，确认当前已烧录映像、已验证事实和未验证风险。
3. 按本文完成接线、安全检查、构建、烧录、校准和数据留档。
4. 每次有效实验都在 `EXPERIMENTS.md` 增加结论；失败实验也要保留 CSV，但必须写明失败原因。

严禁把“代码存在”“构建通过”或“烧录成功”写成“实机通过”。实机通过必须有现场观察，以及对应 CSV、`*.meta.json` 和分析曲线。

## 2. 系统结构

```text
K230 摄像头
  └─识别钢球 X 坐标
       └─UART2 TX(IO5), 921600 baud
            └──────────────> MSPM0 UART3 RX(PA13)
                               ├─钢球位置/速度控制
                               ├─X42S STEP/DIR/EN → 摆杆
                               ├─LF04 四路红外 → 循迹控制
                               ├─双编码器 → 速度与诊断
                               ├─TB6612 → 左右车轮
                               ├─ICM42688 → 加速度诊断
                               ├─OLED → 操作/故障提示
                               └─XDS110 UART → Mac CSV 记录
```

控制闭环必须全部在车上完成。正式比赛时，场外设备只允许接收和录像视频，不得通过无线链路向车上发送控制信息。

## 3. 当前能力边界

| 功能 | 当前结论 | 证据/入口 |
| --- | --- | --- |
| 第三题 `0 → +5 → -5 cm` | 已完成实机验收 | `motor_forward_test/logs/run_20260801_123833.csv` |
| 第三题冻结参数 | `+800 mdeg` 正向巡航、`+12 px` 提前折返、`+565 mdeg` 负向制动 | `ball_beam_vision_control_test.c` |
| 第二题循迹 | 独立映像可构建、烧录；是否实车通过以最新状态文件为准 | `build/line_follow_test.out` |
| 第 4/5/6 问联合控制 | 软件链路、日志和独立映像已具备；必须分项实测 | `build/joint_line_balance_test.out` |
| 联合小球 PD | 当前采用已筛选的 `Kp=11, Kd=1, I=0`；12/14 已实测回摆，不采用 | `joint_line_balance_test.c` |
| 联合车轮控制 | 当前为巡线 PD + S 曲线开环 PWM，编码器用于遥测和故障检测 | `joint_line_balance_test.c` |

任何参数是否仍为当前候选，以 `CURRENT_TASK.md` 和源码为准，不从旧聊天或旧固件哈希反推。

## 4. 关键接线

### 4.1 K230 到 MSPM0

| 信号 | K230 | MSPM0 | 说明 |
| --- | --- | --- | --- |
| 钢球坐标 TX | IO5 / UART2_TX | PA13 / UART3_RX | 单向 UART，921600 baud |
| 地 | GND | GND | 必须共地 |

K230 不需要接收 MSPM0 数据。控制协议为短 ASCII 帧，MSPM0 对超时和陈旧视觉数据执行安全处理。

### 4.2 X42S 摆杆

| 功能 | MSPM0 引脚 |
| --- | --- |
| STEP | PB13 |
| DIR | PB12 |
| EN | PB6 |

上电必须保持步进安全锁定，第一次实体 `S1/START` 后才允许进入受保护的水平标定流程。

### 4.3 LF04、车轮和显示

| 模块 | 连接 |
| --- | --- |
| LF04 O1/O2/O3/O4 | PB18 / PA24 / PA17 / PA12 |
| 左电机 PWM | PB4 |
| 右电机 PWM | PB1 |
| 左编码器 A/B | PB2 / PB24 |
| 右编码器 A/B | PB9 / PA27 |
| OLED SCL/SDA | PB19 / PA15 |
| 实体 S1/START | PB7，对地按下，内部上拉 |

完整针脚、电机方向和电源边界见 `motor_forward_test/H_TASK_1_40引脚总表.md` 与 `motor_forward_test/H_TASK_AI_HANDOFF.md`。

## 5. 上电与烧录安全门

每次烧录前必须逐项确认：

- TB6612 `VM` 已断开，或者左右车轮可靠悬空。
- 钢球已拿走；摆杆附近没有手指、线材或工具。
- X42S 行程没有顶住机械限位。
- MSPM0、K230、驱动器和传感器共地。
- 12 V 只进入允许的电机电源端，绝不进入 MCU 或信号脚。
- 已确认本轮要烧录的映像名称，避免把第三题独立固件与联合固件烧混。

烧录器完成后会提示目标未自动启动。此时按一次 TI 板 `RESET`，不要把 `RESET` 当作 `START`。

## 6. 构建与烧录入口

进入工程：

```sh
cd motor_forward_test
```

### 6.0 最终按钮统一固件

```sh
make -B competition-unified check-host
make flash-competition-unified
```

映像：`build/competition_unified.out`

选择页使用 S2=第二题、S3=第三题、S4=第4/5/6问联合功能，S5保留。
选择后按一次S1确认进入，释放该次S1后，再按所选功能自己的OLED流程操作。
三个任务均复用原控制程序；停止后如需换功能，应人工复位返回选择页。

第二题进入后还需按任务页提示再按一次S1实际发车。计时在实际发车时清零，
在编码器终点停车或运行中S1急停时冻结，OLED的`TIME`页持续显示最终时间。

### 6.1 第三题独立固件

```sh
make -B ball-beam-task3-test check-host
make flash-ball-beam-task3-test
```

映像：`build/ball_beam_task3_test.out`

### 6.2 第二题独立循迹固件

```sh
make -B line-follow check-host
make flash-line-follow
```

映像：`build/line_follow_test.out`

### 6.3 第 4/5/6 问联合固件

```sh
make -B joint-line-balance-test check-host
make flash-joint-line-balance-test
```

映像：`build/joint_line_balance_test.out`

只有构建返回码为 0、全部主机测试通过、XDS110 明确输出 `Program verification successful`，才可进入复位后的实物检查。

## 7. K230 视觉启动检查

项目视觉源文件：

- `motor_forward_test/k230/steel_ball_uart_yolo.py`
- `motor_forward_test/k230/ball_roi_config.py`

当前三点标定：

| 物理位置 | 完整画面 X | 发送给 TI 的偏移 |
| --- | ---: | ---: |
| -5 cm | 84 px | -65 px |
| 0 cm | 149 px | 0 px |
| +5 cm | 208 px | +59 px |

负侧比例约 `13.0 px/cm`，正侧比例约 `11.8 px/cm`，不得擅自改成统一比例。

上电后检查：

1. CanMV/K230 画面中整条 ±5 cm 有效水管可见，上下背景已裁掉。
2. 球在中心时显示 `dX≈0`。
3. 手动放到 +5 cm 和 -5 cm 时，分别接近 `+59` 和 `-65 px`。
4. TI 日志中的 `vision_age_ms` 应小于 150 ms；出现 `LOST` 或持续超时不得启动联合运行。

若修改 K230 文件，必须重新做三点核对；仅看到画框不等于 UART 坐标正确。

## 8. 统一水平标定与按键流程

每次 TI 重新烧录或复位后，软件位置会回到 0，必须重新完成水平标定。

1. 拿走钢球，人工把机构停到下机械端。
2. 第 1 次 `S1`：解锁标定。
3. 第 2 次 `S1`：自动抬升到约 `pos=-565`。
4. 必要时用 `UP/DOWN` 在安全包络内微调水管水平。
5. 第 3 次 `S1`：保存水平；联合固件同时保存当前有效钢球视觉零点。
6. 把钢球轻放到中心，等待视觉稳定，不要沿管推动。
7. 开始正式 CSV 记录。
8. 第 4 次 `S1`：启动本轮自动任务。
9. 运行中再次按 `S1`：急停/受控停止。

第三次保存前，应看到 `pos≈-565`；第四次启动前，球误差应在 ±1 cm 内且视觉新鲜。

## 9. 第三题独立验收

目标：

```text
0 cm → +5 cm → -5 cm
总时长 ≤ 5 s
两个端点误差均 ≤ 1 cm
```

像素验收范围：

- 正端：`+48..+70 px`
- 负端：`-78..-52 px`

正式日志：

```sh
.venv/bin/python tools/record_serial.py --port /dev/cu.usbmodemMG3500011
```

结束后按 `Ctrl-C`，再分析：

```sh
.venv/bin/python tools/analyze_latest.py --logs logs --output analysis
```

已验证样本 `run_20260801_123833.csv` 的结果为：总时长 1.75 s、正端峰值 +66 px、负端停止后 -77..-56 px、丢球 0。冻结参数；若验证重复性，应保持同一固件再跑，不继续调参。

## 10. 联合固件现场流程

联合映像用于把循迹、车轮缓启动、钢球视觉 PD、步进摆杆和安全状态机放到同一程序中。

第一阶段必须架空车轮：

1. 完成第 8 节三次水平标定。
2. 在 `READY` 阶段手动把球移动约 +2 cm 后放手，确认摆杆方向能把球拉回保存点。
3. 确认末段至少 0.5 s 位于 ±1 cm、视觉年龄小于 150 ms、无故障。
4. 接通 TB6612 `VM` 前再次确认车轮可靠悬空。
5. 开始联合 CSV 记录，再按第 4 次 `S1`。
6. 检查左右轮均为前进方向、编码器前进极性符合当前标定、PWM 缓慢上升。
7. 短时间内再次按 `S1`，验证急停可靠。

只有架空方向、编码器、视觉、步进方向和急停全部通过，才允许落地低速短直线。不得直接从桌面构建跳到完整赛道。

## 11. 联合日志与分析

开始记录：

```sh
.venv/bin/python tools/record_joint_serial.py \
  --port /dev/cu.usbmodemMG3500011
```

记录器会同时生成：

- `logs/joint/joint_*.csv`
- `logs/joint/joint_*.meta.json`

分析单条日志：

```sh
.venv/bin/python tools/analyze_joint.py \
  logs/joint/joint_YYYYMMDD_HHMMSS.csv
```

连续运行评分：

```sh
.venv/bin/python tools/score_joint_runs.py \
  logs/joint/joint_*.csv \
  --output analysis/joint/score.json
```

联合任务只有在末尾连续至少 5 次均满足以下条件时，才可标为通过：

- 全程钢球绝对误差不超过 1 cm。
- 正常到达 `STOPPED`，或按当前题目流程完成规定动作。
- `fault_flags=0`。
- 无视觉陈旧样本、无人工碰球、无车轮接触支架等干预。

## 12. 常见故障判断

| 现象 | 优先判断 | 处理 |
| --- | --- | --- |
| CanMV 显示 `SyntaxError` | 上传内容被引号、Markdown 或粘贴边界污染 | 从项目 `.py` 文件重新整体上传，不从聊天代码块复制 |
| TI 串口无输出 | 目标未复位、端口被 IDE 占用或选错串口 | 按 TI `RESET`，关闭占用端口的软件，重新列出 `/dev/cu.usbmodem*` |
| `LOST`/视觉年龄 >150 ms | 球未识别、K230 未运行、UART/GND 问题 | 停止任务，先恢复画面和 UART，不继续用旧坐标 |
| 球不动 | 静摩擦、球面卡点、水平参考偏差 | 拿起并旋转钢球后重放；保留失败 CSV，不立即乱调 PID |
| 球快速冲到另一侧 | 制动幅值/触发时机不合适，或方向错误 | 立即停止；用 CSV 的触发位置和速度分析，一次只改一个参数 |
| 车轮方向错误 | 电机线序/方向定义不一致 | 立即断开 VM；架空修正，禁止落地尝试 |
| 低 PWM 被报堵转 | 编码器低速脉冲间歇导致单样本误判 | 使用当前累计时间窗诊断逻辑，不恢复旧单样本判据 |
| 烧录后电机不动作 | 烧录器未启动目标或仍处于安全锁定 | 先按 `RESET`，再按三次标定流程操作；不要取消安全锁 |

## 13. 每次实验结束必须留下的内容

```text
1. 原始 CSV
2. 同名 *.meta.json
3. 分析 PNG/JSON/摘要
4. 固件或源码 SHA-256
5. 是否有人碰球、车轮是否悬空、VM 是否接通
6. 实际端点/误差/耗时/故障
7. 本次只改了哪些参数
8. 下一步保持什么不变、只验证什么
```

有效结论写入 `PROJECT_STATUS.md`；每次实验写入 `EXPERIMENTS.md`；切换当前任务时更新 `CURRENT_TASK.md`。

## 14. 现场最短检查清单

```text
[ ] 看 CURRENT_TASK，确认映像
[ ] VM 断开或车轮悬空
[ ] 拿走钢球，机械无干涉
[ ] make -B <target> check-host
[ ] flash，并看到 Program verification successful
[ ] TI RESET
[ ] 三次 S1 完成水平保存
[ ] K230 dX≈0、vision_age<150 ms
[ ] 开 CSV
[ ] 第四次 S1 启动
[ ] 随时能用 S1 急停
[ ] Ctrl-C 封存日志
[ ] 生成分析图和摘要
[ ] 更新 EXPERIMENTS / PROJECT_STATUS
```
